name: Exécuter le script Selenium

on: [push]

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pip install selenium selenium-wire blinker

    - name: Run script.py
      run: python .github/workflows/script.py
